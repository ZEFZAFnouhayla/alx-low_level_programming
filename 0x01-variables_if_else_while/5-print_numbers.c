#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - A program that print all single digit numbers
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 10; i++)
	{
	printf("%d", i);
	}
	return (0);
}
